<!DOCTYPE html>
<html>
  <head>
    <meta charset="urf-8">
    <title>会員登録確認</title>
  </head>
  <body>
    <form method="post" action="confirm.php">
      <table>
        <tr>
          <th>ユーザー名</th>
          <td>{{dataArr.user_name}}</td>
        </tr>
        <tr>
          <th>メールアドレス</th>
          <td>{{dataArr.email}}</td>
        </tr>
        <tr>
          <th>生年月日</th>
          <td>{{dataArr.year}}年 {{dataArr.month}}月 {{dataArr.day}}日</td>
        </tr>
      </table>
      <div>
        <input type="submit" name="back" value="戻る">
        <input type="submit" name="complete" value="登録完了">
        {% for key, value in dataArr %}
        {# TODO: trafficないので不要？ #}
        {# 下記メソッドで配列か否かをチェックしている #}
        {# iterableで配列かどうかチェック、、、今回配列はtrafficだけ、trafficの場合はkeyがtraffic[]となる #}
          {% if value is iterable %}
            {% for v in value %}
              <input type="hidden" name="{{key}}[]" value="{{v}}">
              {# name=traffic[] #}
            {% endfor %}
          {% else %}
            <input type="hidden" name="{{key}}" value="{{value}}">
          {% endif %}
        {% endfor %}
      </div>
    </form>
  </body>
</html>