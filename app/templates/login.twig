<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>ログイン</title>
	</head>
	<body>
		<ul id="topicpass">
			<li>
				<a href="index.php">HOME >
				</a>
			</li>
			<li>ログイン</li>
		</ul>
		<form action="confirm.php" method="post">
			<table>
				<tr>
					<th>ユーザー名<span class="red">*</span>
					</th>
					<td>
						<input type="text" name="user_name" value="{{dataArr.user_name}}">
						{% if errArr.user_name | length > 0 %}<br><span class="red">{{errArr.user_name}}</span>
						{% endif %}
					</td>
				</tr>
				<tr>
					<th>パスワード<span class="red">*</span>
					</th>
					<td>
						<input type="password" name="password">
						{% if errArr.password | length > 0 %}
							<br><span class="red">{{errArr.password}}</span>
						{% endif %}
					</td>
				</tr>
			</table>
			<div>
				<input type="submit" value="ログイン" name="login">
				<input type="submit" value="戻る" name="back">
			</div>
		</form>
		<a href="regist.php">
			会員登録はこちらから
		</a>
	</body>
</html>
